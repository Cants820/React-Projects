const numArr = [1, 2, 4, 6, 8, 0];

// evaluates all elements and returns new array given the condition is true;
const filteredArr = numArr.filter((num) => num > 4);

console.log("filtered arr", filteredArr);
